/**
 * @labnoteo/core/node — Node-only helpers (fs-backed adapters, path helpers).
 *
 * Consumed by the VS Code extension host. NOT safe for the browser webview.
 */

export { NodeFileSystem } from './nodeFileSystem';
