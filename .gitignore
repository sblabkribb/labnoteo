node_modules/
dist/
coverage/
*.vsix
*.stackdump

# Obsidian plugin build output (regenerated by esbuild)
/main.js
/main.js.map

# Automation script build output (stage 1; embedded into main.js by esbuild)
/dist-automation/

# Local Obsidian test vault symlink, if any
/test-vault

# Windows bash `2>nul` redirection occasionally leaves a stray file named `nul`.
/nul
